<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Plateforme Séminaires{% endblock %}</title>
    <link rel="icon" type="image/jpeg" href="{{ asset('images/iconeimsp.jpg') }}">

    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
        <style>
            footer {
                background-color: #fd7e14; /* orange Bootstrap */
                color: white;
                padding: 2rem 0;
                margin-top: 4rem;
            }

            footer a {
                color: white;
            }

            footer a:hover {
                text-decoration: underline;
                color: #fff3e0;
            }

            .footer-icon {
                font-size: 1.2rem;
                margin-right: 8px;
            }

            .footer hr {
                border-color: rgba(255, 255, 255, 0.3);
            }
        </style>
    {% endblock %}
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center" href="{{ path('app_home') }}">
            <img src="{{ asset('images/logoimsp.jpg') }}" alt="Logo IMSP" height="40" class="me-2">
            Séminaires IMSP
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {% if app.user %}
                    <li class="nav-item">
                        <span class="nav-link disabled">Connecté : {{ app.user.email }}</span>
                    </li>

                    {% if 'ROLE_ADMIN' in app.user.roles %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('admin_dashboard') }}">Dashboard Admin</a>
                        </li>
                    {% elseif 'ROLE_ORGANISATEUR' in app.user.roles %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('organisateur_dashboard') }}">Dashboard Organisateur</a>
                        </li>
                    {% endif %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

<div class="container my-5">
    {% block body %}{% endblock %}
</div>

<footer>
    <div class="container">
        <div class="row text-center text-md-start">
            <div class="col-md-4 mb-4 mb-md-0">
                <h5>Institut IMSP</h5>
                <p><i class="bi bi-geo-alt footer-icon"></i> Dangbo, Ouémé, Bénin</p>
                <p><i class="bi bi-globe footer-icon"></i><a href="https://www.imsp-benin.com" class="footer-link" target="_blank">www.imsp-benin.com</a></p>
            </div>
            <div class="col-md-4 mb-4 mb-md-0">
                <h5>Contacts</h5>
                <p><i class="bi bi-envelope footer-icon"></i> Secrétariat : <a href="mailto:secretariat@imsp-uac.org" class="footer-link">secretariat@imsp-uac.org</a></p>
                <p><i class="bi bi-envelope-at footer-icon"></i> Développeur : <a href="mailto:ogawin.oro@gmail.com" class="footer-link">ogawin.oro@gmail.com</a></p>
            </div>
            <div class="col-md-4">
                <h5>Plateforme Séminaires</h5>
                <p>Cette plateforme permet la gestion des séminaires, la soumission, validation et diffusion des informations scientifiques à l'IMSP.</p>
            </div>
        </div>

        <hr class="my-4">

        <div class="text-center">
            <small>&copy; {{ "now"|date("Y") }} Institut de Mathématiques et de Sciences Physiques (IMSP) - Tous droits réservés.</small>
        </div>
    </div>
</footer>

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.7/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
{% endblock %}
</body>
</html>
