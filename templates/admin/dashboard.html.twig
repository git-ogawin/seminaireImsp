{% extends 'base.html.twig' %}

{% block title %}Tableau de bord Admin{% endblock %}

{% block body %}
<div class="container my-5">
    <!-- En-tête -->
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="text-primary fw-bold">🎛️ Tableau de bord Administrateur</h1>
        <div class="text-end">
            <p class="mb-1">
                <i class="bi bi-person-circle"></i>
                Connecté en tant que : <strong>{{ app.user.email }}</strong>
            </p>
            <a href="{{ path('app_logout') }}" class="btn btn-outline-danger btn-sm">
                <i class="bi bi-box-arrow-right me-1"></i> Déconnexion
            </a>
        </div>
    </div>

    <!-- Message d’introduction -->
    <div class="alert alert-info shadow-sm">
        🛠️ Utilisez les options ci-dessous pour gérer les séminaires, les utilisateurs et consulter les rapports.
    </div>

    <!-- Cartes de navigation -->
    <div class="row g-4 mt-4">
        <!-- Séminaires -->
        <div class="col-md-4">
            <a href="{{ path('admin_manage_seminars') }}" class="text-decoration-none">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body text-center py-4">
                        <i class="bi bi-calendar-event fs-1 text-primary mb-3"></i>
                        <h5 class="card-title">Gérer les Séminaires</h5>
                        <p class="card-text text-muted">Créer, valider et planifier les présentations.</p>
                    </div>
                </div>
            </a>
        </div>

        <!-- Utilisateurs -->
        <div class="col-md-4">
            <a href="{{ path('admin_manage_users') }}" class="text-decoration-none">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body text-center py-4">
                        <i class="bi bi-people fs-1 text-secondary mb-3"></i>
                        <h5 class="card-title">Gérer les Utilisateurs</h5>
                        <p class="card-text text-muted">Consulter, modifier et attribuer les rôles.</p>
                    </div>
                </div>
            </a>
        </div>

        <!-- Statistiques -->
        <div class="col-md-4">
            <a href="{{ path('admin_reports') }}" class="text-decoration-none">
                <div class="card border-0 shadow-sm h-100">
                    <div class="card-body text-center py-4">
                        <i class="bi bi-bar-chart-line fs-1 text-success mb-3"></i>
                        <h5 class="card-title">Rapports & Statistiques</h5>
                        <p class="card-text text-muted">Visualiser les données des séminaires et des activités.</p>
                    </div>
                </div>
            </a>
        </div>
    </div>
</div>
{% endblock %}
