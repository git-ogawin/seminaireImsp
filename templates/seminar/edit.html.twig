{% extends 'base.html.twig' %}

{% block title %}Modifier le séminaire{% endblock %}

{% block body %}
<h1>Modifier le séminaire "{{ seminar.title }}"</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary mt-3">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('admin_dashboard') }}" class="btn btn-link mt-3">Retour à la liste</a>
{% endblock %}
